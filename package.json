{
  "scripts": {
    "build": "npx tsc",
    "copyInputFiles": "cp -r input/ build/",
    "test": "npm run build && npm run copyInputFiles && node --test build/"
  },
  "devDependencies": {
    "@types/node": "^20.10.1",
    "typescript": "^5.3.2"
  }
}
